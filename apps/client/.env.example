# Нужно только для локальной разработки: Vite слушает свой порт,
# поэтому клиенту надо указать, где сервер.
# В продакшене клиент раздаётся тем же сервером и находит его сам.
VITE_SERVER_URL=http://localhost:2567
